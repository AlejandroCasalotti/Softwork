<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Refresh OAuth Tokens -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_refresh_tokens" model="ir.cron">

        <field name="name">
            Mercado Libre - Refresh OAuth Tokens
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">30</field>

        <field name="interval_type">minutes</field>

        <field name="priority">1</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_refresh_tokens()
        </field>

    </record>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Import Products -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_import_products" model="ir.cron">

        <field name="name">
            Mercado Libre - Import Products
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">30</field>

        <field name="interval_type">minutes</field>


        <field name="priority">5</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_import_products()
        </field>

    </record>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Synchronize Products -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_sync_products" model="ir.cron">

        <field name="name">
            Mercado Libre - Synchronize Products
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">30</field>

        <field name="interval_type">minutes</field>


        <field name="priority">5</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_sync_products()
        </field>

    </record>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Import Orders -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_import_orders" model="ir.cron">

        <field name="name">
            Mercado Libre - Import Orders
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">2</field>

        <field name="interval_type">minutes</field>


        <field name="priority">10</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_import_orders()
        </field>

    </record>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Synchronize Orders -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_sync_orders" model="ir.cron">

        <field name="name">
            Mercado Libre - Synchronize Orders
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">2</field>

        <field name="interval_type">minutes</field>


        <field name="priority">10</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_sync_orders()
        </field>

    </record>

        <!-- ============================================================= -->
    <!-- Mercado Libre - Import Shipments -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_import_shipments" model="ir.cron">

        <field name="name">
            Mercado Libre - Import Shipments
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">5</field>

        <field name="interval_type">minutes</field>


        <field name="priority">15</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_import_shipments()
        </field>

    </record>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Synchronize Shipments -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_sync_shipments" model="ir.cron">

        <field name="name">
            Mercado Libre - Synchronize Shipments
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">5</field>

        <field name="interval_type">minutes</field>


        <field name="priority">15</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_sync_shipments()
        </field>

    </record>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Retry Failed Orders -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_retry_failed_orders" model="ir.cron">

        <field name="name">
            Mercado Libre - Retry Failed Orders
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">15</field>

        <field name="interval_type">minutes</field>


        <field name="priority">20</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_retry_failed_orders()
        </field>

    </record>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Retry Failed Shipments -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_retry_failed_shipments" model="ir.cron">

        <field name="name">
            Mercado Libre - Retry Failed Shipments
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">15</field>

        <field name="interval_type">minutes</field>


        <field name="priority">20</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_retry_failed_shipments()
        </field>

    </record>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Cleanup -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_cleanup" model="ir.cron">

        <field name="name">
            Mercado Libre - Cleanup
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">1</field>

        <field name="interval_type">days</field>


        <field name="priority">50</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_cleanup()
        </field>

    </record>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Health Check -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_health_check" model="ir.cron">

        <field name="name">
            Mercado Libre - Health Check
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">6</field>

        <field name="interval_type">hours</field>


        <field name="priority">90</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_health_check()
        </field>

    </record>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Statistics -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_statistics" model="ir.cron">

        <field name="name">
            Mercado Libre - Statistics
        </field>

        <field name="active">True</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">1</field>

        <field name="interval_type">days</field>


        <field name="priority">95</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_statistics()
        </field>

    </record>

    <!-- ============================================================= -->
    <!-- Mercado Libre - Full Synchronization (Optional) -->
    <!-- ============================================================= -->

    <record id="ir_cron_ml_full_sync" model="ir.cron">

        <field name="name">
            Mercado Libre - Full Synchronization
        </field>

        <field name="active">False</field>

        <field name="user_id" ref="base.user_root"/>

        <field name="interval_number">1</field>

        <field name="interval_type">hours</field>


        <field name="priority">100</field>

        <field name="model_id"
               ref="model_sce_ml_cron"/>

        <field name="state">code</field>

        <field name="code">
model.cron_all()
        </field>

    </record>

</odoo>